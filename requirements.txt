# Core Dependencies for MIDI Music Transformer
# PyTorch (must match CUDA version on RunPod)
torch==2.1.0
torchaudio==2.1.0
torchvision==0.16.0

# Transformers & Training
transformers==4.51.3
huggingface-hub==0.30.2

# MIDI Processing
miditok==3.0.5.post1
miditoolkit==1.0.1
mido==1.3.3
pretty_midi==0.2.10
symusic==0.5.7

# Audio (if needed for playback/conversion later)
soundfile==0.13.1

# Progress & Utilities
tqdm==4.67.1
numpy==1.26.4
pandas==2.2.3

# Optional: For faster training
# xformers==0.0.22.post7  # Uncomment if GPU supports it
